global:
  scrape_interval: 30s
  evaluation_interval: 1m

scrape_configs:
    - job_name: 'prometheus'
      static_configs:
        - targets: [${IP_LOCAL},':',${PROMETHEUS_PORT}] 

    - job_name: 'postgres'
      static_configs:
        - targets: [${IP_LOCAL},':',${POSTGRES_PORT}] 